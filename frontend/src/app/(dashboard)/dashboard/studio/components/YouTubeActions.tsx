'use client';

export function YouTubeCaptionsActions() {
  // YouTube actions are now handled in UrlInput component
  // No additional configuration needed for basic YouTube captions extraction
  return null;
}
